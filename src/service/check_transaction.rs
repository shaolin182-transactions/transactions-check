
/*
For each transactions in database, look for an equivalent in input file

Each input file must match only one bank account

Return a list of transactions in database that were not found in input file
*/
fn check_transations_from_db() {

    // Load all transactions related to a BankAccount

    // Open input file and parse it

    // Loop on transactions from database and look for an equivalent in input file
    // Cost must at least be identical and date has to match (+/- 1 month)
    // if a match is found, transaction from input file must be marked as found for not reusing it

}

/*
For each transactions in input file, look for an equivalent in database
*/
fn check_transations_from_file() {

}