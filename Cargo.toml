[package]
name = "transactions-check"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio   = { version = "1", features = ["full"] }

[dependencies.sea-orm]
version = "2.0.0-rc"
features = [
    "sqlx-postgres",
    "runtime-tokio-rustls",
    "macros",
    "debug-print",
    "entity-registry",
    "schema-sync",
]
